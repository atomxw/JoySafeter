name: base64_codec
category: basic
tags: [ctf, encoding, crypto, base64]
endpoint: api/tools/base64_codec
description: 'Encode or decode Base64 strings. Useful for CTF challenges involving encoded data, cookies, tokens, and obfuscated content.'
returns: Encoded or decoded string result
parameters:
- name: input
  type: string
  required: true
  description: The string to encode or decode
- name: operation
  type: string
  required: true
  description: Operation to perform (encode or decode)
