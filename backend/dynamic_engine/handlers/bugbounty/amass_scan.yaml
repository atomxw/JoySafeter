name: amass_scan
category: subdomain_enumeration
tags: [bug_bounty, vulnerability_hunting, penetration_testing, amass]
endpoint: api/tools/amass
description: 'Execute Amass for subdomain enumeration with enhanced logging. domain:
  The target domain mode: Amass mode (enum, intel, viz) additional_args: Additional
  Amass arguments'
returns: Subdomain enumeration results
parameters:
- name: domain
  type: string
  required: true
  description: The target domain
- name: mode
  type: string
  required: false
  description: Amass mode (enum, intel, viz)
  default: enum
- name: additional_args
  type: string
  required: false
  description: Additional Amass arguments
  default: ''
