name: install_python_package
category: unknown
endpoint: api/python/install
description: 'Install a Python package in a virtual environment on the HexStrike server.
  package: Name of the Python package to install env_name: Name of the virtual environment'
returns: Package installation results
parameters:
- name: package
  type: string
  required: true
  description: Name of the Python package to install
- name: env_name
  type: string
  required: false
  description: Name of the virtual environment
  default: default
