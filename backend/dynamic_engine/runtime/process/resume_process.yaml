name: resume_process
category: process_management
endpoint: api/tools/resume_process
description: 'Resume a paused process. pid: Process ID to resume'
returns: Success status of the resume operation
parameters:
- name: pid
  type: integer
  required: true
  description: Process ID to resume
